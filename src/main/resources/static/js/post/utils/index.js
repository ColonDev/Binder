window.PostUtils = {
    setupFilePreview: window.PostFilePreview ? window.PostFilePreview.setupFilePreview : undefined,
    openModal: window.PostModal ? window.PostModal.openModal : undefined,
    closeModal: window.PostModal ? window.PostModal.closeModal : undefined,
    setupCarousel: window.PostCarousel ? window.PostCarousel.setupCarousel : undefined
};
